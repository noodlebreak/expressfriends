/*
 * This services module exists to contain all model
   related service functions separately per model, thereby
   reducing clutter in model and schema modules themselves.
 */
module.exports = require('./users');
